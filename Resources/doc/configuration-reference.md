Full Default Configuration
==========================

```YAML
# app/config/config.yml
tystr_aws:
    access_key:           ~ # Required
    secret_access_key:    ~ # Required
    region:               ~ # Required
    config:               []
    
    s3:
        stream_wrapper:       false
```
